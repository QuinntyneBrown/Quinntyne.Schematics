using {{ rootNamespace }}.Core.Common;

namespace {{ namespace }}
{
    public class {{ namePascalCase }}: DomainEvent
    {
        public {{ namePascalCase }}(Guid? correlationId)
        {
            EventData = new { correlationId };
            EventType = EventTypes.{{ entityNamePascalCasePlural }}.{{ namePascalCase }};
        }
    }
}